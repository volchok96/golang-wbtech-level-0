kafka:
  broker: "localhost:9092"  
  group_id: "my-group"
  topic: "my-topic"   

postgres:
  host: "localhost"
  port: 5432    
  user: "postgres"
  password: "mypass"
  dbname: "orderdb"

memcached:
  host: "localhost"
  port: "11211"
